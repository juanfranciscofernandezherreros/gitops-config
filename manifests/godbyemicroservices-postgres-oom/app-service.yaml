apiVersion: v1
kind: Service
metadata:
  name: godbyemicroservices-postgres-oom
  namespace: godbyemicroservices-postgres-oom
  labels:
    app: godbyemicroservices-postgres-oom
spec:
  selector:
    app: godbyemicroservices-postgres-oom
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
