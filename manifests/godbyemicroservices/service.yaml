apiVersion: v1
kind: Service
metadata:
  name: godbyemicroservices
  namespace: godbyemicroservices
  labels:
    app: godbyemicroservices
spec:
  selector:
    app: godbyemicroservices
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
